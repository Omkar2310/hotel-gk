export class Menu {
  name: string;
  imageLink: string;
  price: number;

  constructor(name: string, imageLink: string, price: number) {
    this.name = name;
    this.imageLink = imageLink;
    this.price = price;
  }
}
